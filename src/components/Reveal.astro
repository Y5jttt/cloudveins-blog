---
// 滚动淡入包裹器 (IntersectionObserver 在 ui.js 中统一处理 .reveal)
interface Props { d?: string; as?: string; class?: string; }
const { d = '0s', as = 'div', class: cls = '' } = Astro.props;
const Tag = as;
---

<Tag class={`reveal ${cls}`} style={`--d:${d}`}>
  <slot />
</Tag>
